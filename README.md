# J2EE Assignment

This repository contains a collection of Java EE (J2EE) web application examples demonstrating **JDBC database access**, **Servlets**, and **JSP pages**. It includes multiple mini-projects used as assignments/exercises for learning Java web development.

## 📌 Overview

This project covers:

✅ JDBC connectivity with a database  
✅ Java Servlets to handle HTTP requests  
✅ JSP (JavaServer Pages) for dynamic content  
✅ Combining Servlet + JDBC + JSP for CRUD operations  

Each folder represents a specific example or use-case such as CRUD operations, login/register flows, session examples, etc.

## 🚀 Features

Here are some examples included:

- **JDBC CRUD** – Connect to a database using JDBC and perform Create, Read, Update, Delete.  
- **Servlet Examples** – Simple servlet demos for understanding request/response lifecycle.  
- **JSP Examples** – Demonstrating JSP tags, scripting, expressions, error/exception handling.  
- **Login & Register** – Simple user registration and login using Servlets + JSP + JDBC.  
- **Session Example** – Handling user sessions in web applications.


## 🧰 Technologies Used

- **Java EE / Jakarta EE**  
- **JDBC API** for database interaction  
- **Servlet API** for handling HTTP requests  
- **JSP** for rendering dynamic responses  
- **Apache Tomcat** (or similar servlet container)  

## 📥 Setup & Installation

### Prerequisites

1. **Java Development Kit (JDK)** installed  
2. **Apache Tomcat** (or another Java EE application server)  
3. **Database** (MySQL / PostgreSQL / any JDBC-compatible DB)  
4. JDBC driver library added in your project’s `WEB-INF/libs`  



## 🗂️ Folder Structure

